import Wrapper from "@/components/layouts/Wrapper";
import React from "react";
import RegisterForm from "../_components/RegisterForm";

const page = () => {
  return <Wrapper className="flex justify-center">
    <RegisterForm/>
  </Wrapper>;
};

export default page;
